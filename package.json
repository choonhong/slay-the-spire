{
  "name": "sts2-tracker",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check:node": "node scripts/check-node.js",
    "dev": "npm run check:node && concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run check:node && npm run build --workspace=backend && npm run build --workspace=frontend",
    "seed": "npm run check:node && npm run seed --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
